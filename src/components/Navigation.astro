---
const navItems = [
  { label: 'For Artists', href: '#for-artists' },
  { label: 'Services', href: '#services' },
  { label: 'Studio Calculator', href: '#calculator', highlight: true },
  { label: 'Success Stories', href: '#testimonials' },
  { label: 'About', href: '#about' },
];
---

<nav class="fixed top-0 left-0 right-0 z-40 bg-white/90 backdrop-blur-md border-b border-gray-200">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex items-center justify-between h-20">
      <!-- Logo -->
      <a href="/" class="flex items-center space-x-3 group">
        <div class="relative">
          <!-- Paint blob background -->
          <div class="absolute inset-0 bg-gradient-to-br from-cadmium-300 to-sienna-400 opacity-20 blur-sm scale-110" style="border-radius: 30% 70% 70% 30% / 30% 30% 70% 70%;"></div>
          <div class="relative w-10 h-10 bg-prussian-700 flex items-center justify-center text-white font-bold text-lg transform group-hover:rotate-3 transition-transform duration-300" style="border-radius: 2px 15px 3px 12px;">
            A
          </div>
        </div>
        <div>
          <div class="font-display text-xl font-bold text-charcoal">ACE Accountable</div>
          <div class="text-xs text-umber-600 -mt-1">Bookkeeping for Creatives</div>
        </div>
      </a>
      
      <!-- Desktop Navigation -->
      <div class="hidden md:flex items-center space-x-6">
        {navItems.map((item) => (
          <a
            href={item.href}
            class={`
              text-sm font-medium transition-all duration-300
              ${item.highlight 
                ? 'text-sienna-400 hover:text-sienna-500 font-semibold' 
                : 'text-charcoal hover:text-prussian-600'
              }
            `}
          >
            {item.label}
          </a>
        ))}
      </div>
      
      <!-- CTA and Contact -->
      <div class="hidden md:flex items-center space-x-6">
        <a href="tel:860-365-1147" class="text-sm font-medium text-umber-600 hover:text-umber-700 transition-colors">
          📞 860-365-1147
        </a>
        <a href="#contact" class="inline-flex items-center px-6 py-2.5 text-sm font-medium text-white bg-prussian-700 hover:bg-prussian-800 transition-all duration-300" style="border-radius: 2px 12px 3px 14px;">
          Book Free Consultation
        </a>
      </div>
      
      <!-- Mobile Menu Button -->
      <button class="md:hidden p-2 rounded-lg hover:bg-umber-50 transition-colors" id="mobile-menu-button">
        <svg class="w-6 h-6 text-charcoal" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
        </svg>
      </button>
    </div>
  </div>
  
  <!-- Mobile Menu -->
  <div class="md:hidden hidden" id="mobile-menu">
    <div class="px-4 pt-2 pb-6 space-y-2 bg-white border-t border-gray-200">
      {navItems.map((item) => (
        <a
          href={item.href}
          class={`
            block px-4 py-3 rounded-xl text-base font-medium transition-all duration-300
            ${item.highlight 
              ? 'bg-coral-400 text-white' 
              : 'text-charcoal hover:bg-sage-50'
            }
          `}
        >
          {item.label}
        </a>
      ))}
      <div class="pt-4 space-y-3">
        <a href="tel:860-365-1147" class="block text-center py-3 text-sage-600 font-medium">
          📞 860-365-1147
        </a>
        <button class="btn-primary w-full">
          Book Free Consultation
        </button>
      </div>
    </div>
  </div>
</nav>

<script>
  const mobileMenuButton = document.getElementById('mobile-menu-button');
  const mobileMenu = document.getElementById('mobile-menu');
  
  mobileMenuButton?.addEventListener('click', () => {
    mobileMenu?.classList.toggle('hidden');
  });
  
  // Close mobile menu when clicking outside
  document.addEventListener('click', (e) => {
    if (!mobileMenuButton?.contains(e.target as Node) && !mobileMenu?.contains(e.target as Node)) {
      mobileMenu?.classList.add('hidden');
    }
  });
</script>